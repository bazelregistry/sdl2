load("@rules_cc//cc:defs.bzl", "objc_library")

objc_library(
	name = "cocoa",
	hdrs = glob(["*.h"]),
	srcs = glob(["*.m"]),
	copts = [
		"-fno-objc-arc",
		"-Isrc/video/khronos",
	],
	deps = [
		"//:SDL2internal",
		"//src/dynapi:headers",
		"//src/video:headers",
		"//src/events:headers",
		"//src/video/khronos:headers",
	],
	visibility = ["//:__subpackages__"],
)

objc_library(
	name = "headers",
	hdrs = glob(["*.h"]),
	visibility = ["//:__subpackages__"],
)
