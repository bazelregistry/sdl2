load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
	name = "jack",
	hdrs = glob(["**/*.h"]),
	srcs = glob(["**/*.c"]),
	deps = [
		"//:SDL2internal",
		"//src/dynapi:headers",
	],
	visibility = ["//:__subpackages__"],
)
