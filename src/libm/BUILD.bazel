load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
	name = "libm",
	hdrs = glob(["*.h"]),
	srcs = glob(["*.c"]),
	alwayslink = True,
	deps = [
		"//:SDL2internal",
		"//src/dynapi:dynapi",
	],
	visibility = ["//visibility:public"],
)
