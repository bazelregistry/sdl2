load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
	name = "generic",
	hdrs = glob(["*.h"]),
	srcs = glob(["*.c"]),
	deps = [
		"//src/dynapi:headers",
		"//src/thread:headers",
		"//:SDL2internal"
	],
	visibility = ["//:__subpackages__"],
)

cc_library(
	name = "headers",
	hdrs = glob(["*.h"]),
	visibility = ["//:__subpackages__"],
)
