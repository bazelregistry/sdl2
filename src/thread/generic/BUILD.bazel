load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
	name = "generic",
	hdrs = glob(["*.h"]),
	srcs = glob(["*.c"]),
	deps = [
		"//:SDL2internal",
		"//src/dynapi:headers",
		"//src/thread:headers",
		"//src/thread/pthread:headers",
	],
	visibility = ["//:__subpackages__"],
)

cc_library(
	name = "headers",
	hdrs = glob(["*.h"]),
	visibility = ["//:__subpackages__"],
)
